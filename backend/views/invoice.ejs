<!DOCTYPE html>
<html>
<head>
    <title>Invoice #<%= order._id %></title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; padding: 20px; color: #333; }
        .container { max-width: 800px; margin: 0 auto; border: 1px solid #ddd; padding: 20px; border-radius: 8px; }
        .header { text-align: center; margin-bottom: 20px; }
        .header h1 { color: #dc3545; font-size: 28px; margin: 0; }
        .brand-section { display: flex; align-items: center; justify-content: center; margin-bottom: 20px; }
        .brand-section img { height: 60px; width: auto; margin-right: 15px; }
        .brand-section h2 { font-size: 24px; font-weight: bold; color: #dc3545; }
        .order-info { margin-bottom: 20px; display: flex; justify-content: space-between; }
        .order-info div { width: 48%; }
        .order-info p { margin: 0; font-size: 14px; line-height: 1.5; }
        .order-info .label { font-weight: bold; color: #555; }
        .items-table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        .items-table th, .items-table td { border: 1px solid #eee; padding: 10px; text-align: left; font-size: 14px; }
        .items-table th { background-color: #f8d7da; color: #dc3545; }
        .items-table .text-right { text-align: right; }
        .total { text-align: right; font-size: 16px; margin-top: 10px; }
        .total .label { font-weight: bold; }
        .footer { text-align: center; margin-top: 30px; font-size: 12px; color: #888; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="brand-section">
                <h2>Kitsune Store</h2>
            </div>
            <h1>Invoice</h1>
        </div>
        
        <div class="order-info">
            <div>
                <p class="label">Billed To:</p>
                <p><%= order.userName %></p>
                <p><%= order.userEmail %></p>
                <p><%= order.address.street %>, <%= order.address.city %>, <%= order.address.state %>, <%= order.address.zipcode %></p>
            </div>
            <div>
                <p class="label">Order Date:</p>
                <p><%= new Date(order.date).toLocaleDateString('en-GB') %></p>
                <p class="label" style="margin-top: 10px;">Status:</p>
                <p><%= order.status %></p>
            </div>
        </div>

        <table class="items-table">
            <thead>
                <tr>
                    <th>Product</th>
                    <th>Size</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th class="text-right">Total</th>
                </tr>
            </thead>
            <tbody>
                <% order.items.forEach(item => { %>
                    <tr>
                        <td><%= item.name %></td>
                        <td><%= item.size %></td>
                        <td><%= item.quantity %></td>
                        <td>₹<%= item.price %></td>
                        <td class="text-right">₹<%= item.price * item.quantity %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="total">
            <p class="label">Subtotal: ₹<%= order.amount %></p>
        </div>
        <div class="total">
            <p class="label">Shipping: ₹20</p>
        </div>
        <div class="total">
            <p class="label">Grand Total: ₹<%= order.amount + 20 %></p>
        </div>

        <div class="footer">
            <p>&copy; 2024 Kitsune Store. All rights reserved.</p>
        </div>
    </div>
</body>
</html>